
/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package br.edu.iff.quissama.cj_is_back;

import javax.swing.JOptionPane;

/**
 * Este código foi importado de um programa escrito em C por um ser aleatório;
 *
 * @author aluno
 */
public class Códigoaleatório {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        int a = 0, b, c, x = 0, y = 1, z, w = 0, soma = 0, i = 0;

        while (a < 4) {

            System.out.print("Apresentar os numeros da série Fibonacci ===== 1\n");
            System.out.print("Apresentar numeros primos ===== 2\n");
            System.out.print("Apresentar numeros perfeitos ===== 3\n");
            System.out.print("Sair do programa ===== 4\n");
            JOptionPane.showInputDialog("Confirme a sua opção: ");
        }

        System.out.print(i + "\n");
        //O valor de i e depois pula uma linha;
        System.out.print("O valor de i é " + i);
        // mostra a frase "O valor de i é " (tem que te um espaço para o valor da variavel depois do (é))e depois o valor de i 
        System.out.print("i é " + i + "! =D \n");
        System.out.println(soma);
        
        
        
        String JOptionPane.showInputDialog (string);
        String oqOUsuarioDigitar  = JOptionPane.showInputDialog();
        
        a = Integer.parseInt(oqOUsuarioDigitar);
        
        scanf("%c", c);
        // Conversão:
        char umChar = oqOUsuarioDigitar.charAt(0);
        // "Conversão", pegar o valor de uma string e guardar numa variavel int;
        String yolo = JOptionPane.showInputDialog();
        int foo = integer.parseInt(yolo);
        // "Conversão", pegar o valor de uma string e guardar numa variavel float;
        float baz = Float.parseFloat(yolo);
        // "Conversão", pegar o valor de uma string e guardar numa variavel char;
        char bar = yolo.charAt(0); // "(0)" = a primeira possisão do char;
        System.out.println("Yolo = " + foo);
        
    }

}
